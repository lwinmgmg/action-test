name: learn-github-actions
on: [push]

jobs:
  check-links:
    runs-on: ubuntu-latest
    environment: Docker
    steps:
      - name: testing
        uses: actions/checkout@v3
      - name: run
        run: |
          ls -ahl
          pwd
          go version
          echo $MY_SECRET